Flask==3.1.3
Flask-Limiter==4.1.1
python-dotenv==1.2.2
gunicorn==21.2.0
python-docx
requests
openpyxl
anthropic
markdown-pdf
pypdf
ddgs
pytest>=9.0
resend
celery==5.4.0
redis==5.0.7
hvac>=2.0.0
psycopg2-binary>=2.9.9

# Keycloak JWT verification (Phase 2 of SECURITY_MIGRATION_KEYCLOAK.md).
# python-jose is FOSS-compatible (MIT) -- chosen over python-keycloak to
# avoid a heavy dependency tree. The [cryptography] extra pulls in PyCA
# cryptography for RS256 verification.
python-jose[cryptography]>=3.3.0
# Direct dependency since 2026-07-18: secrets_file.py encrypts .env at rest
# with Fernet. It arrived transitively via python-jose before; a transitive
# dependency that a security feature relies on is one refactor from vanishing.
cryptography>=42.0.0

# SOC 2 M3.4 observability -- Prometheus client for /metrics exposure
# (counters / histograms / gauges). FOSS (Apache-2.0). The library is
# stateless; metrics live in module-global registries reset per process.
prometheus-client>=0.20.0

